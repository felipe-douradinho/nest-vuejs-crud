import { defineComponent } from "vue";
import KnightList from "../../../Knights/components/list/KnightList.vue";

export default defineComponent({
    name: "HeroList",
    components: {
        KnightList,
    }
});